# Build 546: map controls and area-effect targeting

This is the 1.0.7 hotfix line based on the released 1.0.6 runtime, without immersive
town services. Builds 538–545 and transport IDs 78–81 / messages 19–22 remain reserved
for `feature/immersive-town-services`.

## Findings and changes

- The 3D button rail enumerated `UIGuildmasterButton`; the campaign city-event button
  is a separate `UICityEncounterButton`. Native `UIGuildmasterHUD.Init` explicitly
  enables that control only for campaign mode. Guildmaster has no city-event button.
  Its `GUIAnimator`, image state and native interaction restrictions must follow the
  original control. The event opens `UIEventPanel`, already shared as `Encounter`.
  Existing `EncounterChoice` forwards client choices to the modded host, validates
  event/screen/option identity, and uses the native replicated continuation for everyone.
- `ReleaseMapRoomFloats` called native `Hide()` even for a presentation-only map
  switch. Returning to 3D emits no new native `Show` edge. Preserve native state on
  that exit and re-enroll the original character/quest windows on 3D entry until
  conversion succeeds. Native tutorial unlock and story/journey restrictions remain.
- AoE rotation already used horizontal movement of the stick opposite the turning
  hand (default left). The extra derived `BoardTargeting` requirement excluded
  native object-placement and damage targeting that can remain in choreography
  `Play`. Native display state, acting-player authority and modal restrictions decide
  eligibility. The maintainer rejected competing with locomotion: short B/Y releases
  now rotate clockwise/counterclockwise by default. Both sticks remain available;
  the B+Y recenter chord and long or interrupted holds never rotate. The legacy stick
  binding is an explicit option under Comfort > Hands & aiming. Exact native Tinkerer
  hints name the active binding in English and German, including changes while open.
- Map-button hints retain native presentation, placed flat beside the hovered cap.
  A dedicated bounded presentation stream (message 23 / fragments 24 / TLV 82)
  carries complete pictures and explicit disappearance, through the existing
  compression and 864-byte / 50 ms aggregate event scheduler. It does not send game
  actions. Old protocol layouts and the version byte are unchanged.

## Evidence limits

The current main `LogOutput.log` identifies NPC build 544; the remote log identifies
historical build 500. These are not runtime confirmation of this hotfix. Causes above
are established from production/native source; headset presentation remains unverified.

## Hardware checks

1. Campaign: city-event cap visible, correct enabled/disabled state and native pulse;
   open the encounter, let both host and client choose options and finish it.
   Guildmaster: no city-event cap.
2. Switch 2D/3D repeatedly (including more than four cycles), both map modes and an
   initial 2D load. Character and quest windows remain usable. Story/loadout must
   retain their intentional window visibility and continue normally.
3. Hover ordinary and disabled map caps: native hints lie flat beside the cap,
   disappear correctly, and are identical for peers, including overlapping hovers.
   Check a late join, scene departure and 2D/3D switches while a hint is visible.
4. Tinkerer rotation tutorial: B rotates clockwise and Y counterclockwise in 60-degree
   steps on short release. Both sticks retain locomotion, turning and optional vertical
   movement. Check rapid B/Y alternation, long holds, both recenter release orders and
   leaving targeting while holding a button. Change the rotation setting while the hint
   is open: its instruction updates immediately. For the optional stick binding, check
   swapped turn hand, turning disabled and stick-click world grabbing. Exercise native
   object-placement effects, melee facing and non-acting spectators; confirm the selected
   rotated effect normally on the control board.

## Validation

- Strict Release builds without warnings or errors. The integrated real-runtime wire
  suite passes 254,629 assertions, including the new stream under the existing shared
  event budget, fragmentation, compression, batching and topology boundaries.
- Targeted production harnesses: AoE 353 assertions / 11 rejected negative controls;
  city eligibility 1,043 / 2; permanent map-window lifecycle 1,477 / 9; tooltip driver
  1,039 / 8; native tooltip presentation 598 / 6.
- The complete required runtime suite was executed. The guard's original sequential
  run stopped at a stale Guildmaster rail source assertion; the remaining harnesses
  were run separately rather than repeating successful tests. Updated AoE tests and
  affected source gates were rerun after the maintainer changed the input requirement.
- Source surfaces: 626 config keys, 174 registered patches and 4,744 log tokens; no
  removals. Patch inventory: 132 classes / 200 methods. Defaults, wire coverage,
  diagnostic writes, partial initialization, enum arrays and bundle format pass.
  Bilingual player documentation, workflow Actionlint, 36 release-topology assertions
  and whitespace checks pass. Final menu coverage keeps the optional stick sensitivity
  and repeat interval beside the binding; no new split family or raw enum labels.
- The corrected Guildmaster rail check passes 10,338 layout assertions, 25 source
  bindings and 10 negative controls. It requires both mode-button and city-button
  discovery to be empty before treating the rail as empty.
- The guard's compiled comparison runs separately after these gates. It reports the
  expected cumulative difference against historical baseline `080c505e9`: 108 changed
  and 88 added/removed entries, including this task's input, map and transport types.
  This is a behavior-changing hotfix, not an empty-diff refactor.

Automated checks establish the covered source/runtime contracts, not headset visuals.

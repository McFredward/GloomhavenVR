# Latch arrays whose literal length disagrees with the enum that indexes them — the sites a
# review has already convicted and a lane is repairing.
#
# LAST VERIFIED 2026-09-08 against 49ceab21 (ModBuild 483): this file has ZERO live entries.
# `python3 scripts/check-enum-arrays.py` -> "15 literal-sized array(s) index an enum declared in
# this repository; 15 agree with their enum's member count, 0 excused in ENUM-ARRAYS.allow."
# The file did its job and emptied, exactly as its "TEMPORARY BY CONSTRUCTION" note intends.
# Do not delete it: the gate reads it, and an empty allow-list is the correct steady state.
#
# One line per (file, field):    Net/Remote/File.cs | fieldName | the finding, and the build
#
# THIS FILE IS TEMPORARY BY CONSTRUCTION. check-enum-arrays.py FAILS on a line whose site is
# no longer a disagreement, so deleting the line is the last step of the fix and cannot be
# forgotten. Nothing else belongs here: a NEW disagreement is a build failure, which is the
# whole point — this class shipped silently once and cost two of six card-FX surfaces.
#
# EMPTY IS THE CORRECT STATE, and it is how this file shipped its first build. The four entries
# it was created with were R2 F1 of the 2026-09-07 review round — the four RemoteCardArt latch
# arrays left at a literal 4 when FxSurface grew to six, killing the Active and Held card-FX
# surfaces with nothing in the log at the shipped tier. Fix lane A closed them in ModBuild 480
# by deriving every length from Enum.GetValues and routing all four latch sites through one
# bounds-checked helper, so an instrument can no longer kill the thing it measures. The gate
# then failed on the four now-stale lines, which is exactly what it is for, and they are gone.
